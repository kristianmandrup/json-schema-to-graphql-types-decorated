class BaseEntityModel {
  constructor({shape, config}) {
    this.shape = model
    this.config = config
    this.value = shape.value
    this.db = this.value.db
  }
}

model.exports = {
  BaseEntityModel
}
